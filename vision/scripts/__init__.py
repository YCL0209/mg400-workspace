"""Operator / verification scripts.

- ``handeye_verify.py`` (V3, = M0d): known-object projection vs actual
  placement sanity check. Depends on M0c artifact landing.
- ``pick_demo.py`` (V8): end-to-end demo (image -> detection -> base
  coordinate -> arm in position). Closing P10 milestone.
"""
