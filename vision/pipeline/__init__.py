"""CV pipeline (V4-V5).

Will contain ``camera_source.py`` (frame iterator over DeltaCamera),
``detector.py`` (AOI / PatchCore / anomalib inference), and
``output.py`` (Detection dataclass schema). Phase5-panel CV code is
migrated into ``detector.py`` at V5.
"""
